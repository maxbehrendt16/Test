name: Classify Properties

on:
  push:
    paths:
      - "property_classifier/input_properties.csv"
  workflow_dispatch:

jobs:
  classify:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r property_classifier/requirements.txt

      - name: Run property classifier
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        working-directory: property_classifier
        run: python classifier.py --input input_properties.csv --output classified_results.csv

      - name: Upload results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: classified-results
          path: property_classifier/classified_results.csv
          retention-days: 30

      - name: Print results to log
        run: cat property_classifier/classified_results.csv
